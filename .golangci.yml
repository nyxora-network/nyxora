run:
  go: "1.25"
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

linters-settings:
  staticcheck:
    checks: ["all"]

issues:
  exclude-use-default: false
